export type Service = {
  description: string;
  icon: React.ReactNode;
  title: string;
  image: string;
};

export type Testimonial = {
  image: string;
  name: string;
  position: string;
  text: string;
};
